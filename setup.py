# -*- coding: utf-8 -*-
"""Setup script for TracEpicPlugin.

Most of the packaging metadata lives in ``setup.cfg``.  This thin
``setup.py`` is kept for editable installs (``pip install -e .``) and for
older tooling that still expects a ``setup.py`` to be present.
"""

from setuptools import setup

setup()
